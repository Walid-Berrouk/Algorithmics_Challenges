ALGORITHM Somme2
VAR
    a, b, c, sum : INTEGER
BEGIN
    // Solution One
    Read(a);
    Read(b);
    Read(c);

    // Solution Two
    Read(a, b, c);

    // Solution Three
    Write("Enter First Value : ");
    Read(a);
    Write("Enter Second Value : ");
    Read(b);
    Write("Enter Third Value : ");
    Read(c);

    IF ((a > 0) AND (b > 0) AND (c > 0)) THEN
        sum := a + b + c;
        Write("La Somme des trois nombre est : ", sum);
    ELSE
        Write("You have entered Wrong Values !");
    END_IF

    
END